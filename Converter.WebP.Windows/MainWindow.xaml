<metro:MetroWindow x:Class="Converter.WebP.Windows.MainWindow"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:local="clr-namespace:Converter.WebP.Windows"
                   mc:Ignorable="d"

                   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
                   xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                   TextElement.Foreground="{DynamicResource MaterialDesignBody}"
                   Background="{DynamicResource MaterialDesignPaper}"
                   TextElement.FontWeight="Medium"
                   TextElement.FontSize="14"
                   FontFamily="{materialDesign:MaterialDesignFont}"

                   AllowDrop="True"

                   Title="WebP Converter" Height="720" Width="1280">
    <Grid>
        <ListView Name="ImageListView" Drop="ImageListView_OnDrop">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Name}"
                        Header="Name" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Type}"
                        Header="Type" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Size}"
                        Header="Size" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding ConvertedSize}"
                        Header="Converted Size" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Location}"
                        Header="Location" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Name="ConvertButton" Content="Convert" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,10,10" Click="ConvertButton_OnClick"/>
    </Grid>
</metro:MetroWindow>
